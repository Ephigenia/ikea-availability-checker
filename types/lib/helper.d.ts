/**
 * Normalize the buCode
 *
 * Note that the buCode "003" must keep the zeros
 *
 * @param {string} buCode
 * @returns {string}
 */
export function normalizeBuCode(buCode: string): string;
/**
 * Normalize the buCode
 *
 * Note that the buCode "003" must keep the zeros
 *
 * @param {string} buCode
 * @returns {string}
 */
export function normalizeBuCode(buCode: string): string;
/**
 * @param {string} productId
 * @returns {string}
 */
export function normalizeProductId(productId: string): string;
/**
 * @param {string} productId
 * @returns {string}
 */
export function normalizeProductId(productId: string): string;
//# sourceMappingURL=helper.d.ts.map